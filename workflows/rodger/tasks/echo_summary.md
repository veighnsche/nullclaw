# echo_summary

Status: scaffolded

Goal:
- Return a short deterministic summary for a text input.

Inputs:
- `task_id`
- `prompt`

Output:
- `terminal_status`: `succeeded` or `failed`
- `summary_text`
