# elevenlabs integration

Purpose:

- voice profile generation
- speech synthesis for call flows

Keep this adapter product-scoped:

- no direct imports into core runtime internals
- no hardcoded user identity or prompt policy

Required secrets (example names):

- `ELEVENLABS_API_KEY`
